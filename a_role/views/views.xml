<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="a_role.list">
      <field name="name">a_role list</field>
      <field name="model">a_role.a_role</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="a_role.action_window">
      <field name="name">a_role window</field>
      <field name="res_model">a_role.a_role</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="a_role.action_server">
      <field name="name">a_role server</field>
      <field name="model_id" ref="model_a_role_a_role"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="a_role" id="a_role.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="a_role.menu_1" parent="a_role.menu_root"/>
    <menuitem name="Menu 2" id="a_role.menu_2" parent="a_role.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="a_role.menu_1_list" parent="a_role.menu_1"
              action="a_role.action_window"/>
    <menuitem name="Server to list" id="a_role" parent="a_role.menu_2"
              action="a_role.action_server"/>
    -->
  </data>
</odoo>